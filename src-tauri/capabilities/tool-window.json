{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "tool-window",
  "description": "Generated tool-* windows: allowlist-only via coreside-tool-scoped (no core:default, no backup/restore/credentials). Do NOT link coreside-tool-deny-sensitive — Tauri 2.11 applies commands.deny globally and would block main.",
  "windows": ["tool-*"],
  "permissions": [
    "coreside-tool-scoped",
    "core:event:default",
    "core:window:allow-close",
    "core:window:allow-set-title",
    "core:window:allow-show",
    "core:window:allow-hide",
    "core:window:allow-set-focus"
  ]
}

